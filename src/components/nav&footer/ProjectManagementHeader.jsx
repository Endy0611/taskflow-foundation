import React from "react";

const ProjectManagementHeader = ({ headerText }) => {
  return (
    <div className="mb-2">
      <h2 className="font-semibold text-lg">{headerText}</h2>
    </div>
  );
};

export default ProjectManagementHeader;
